- ~~read each and evry code / md file and make the wording as if we're passing this project to someone - currently it often sounds like an AI coding agent (you) is talking to me (who build it while studying, learning, asking questions)~~
  **Done (2026-08-24):** read the full README, the highest-prose concept doc (`docs/concepts/03-reliability-debugging-ops.md`), and grepped all 143 real code/doc files (excluding vendored `.venv`) for the specific tells this note describes (direct references to "what you asked," conversational hand-offs, "while I was learning" framing, assistant-style preambles). Found and fixed 3 real instances, all in `README.md`: the TOC entry, the in-body cross-reference, and the section heading all read "...concepts you asked about" — a direct callback to the live Q&A that built this doc, now just "...concepts." Everything else that matched a first-pass grep (the 400+ `you`/`your` hits) turned out to be ordinary second-person technical-writing address ("if you run this on...", "you'll see...") — normal and fine in this kind of doc, not the pathology this note was flagging. The concept docs, operations docs, and service READMEs were already in professional, handoff-ready voice — no further changes needed there.
- AIOps (here: ops-for-the-AI-system, i.e. classic ops applied to this AI stack — see docs/concepts/02-llmops-mlops-tooling.md §1 for why that's a different thing from "AI-for-ops") -> implement both! we have AI and all the dashboards/data available with us and let's apply AI-for-ops
- any other improvements
